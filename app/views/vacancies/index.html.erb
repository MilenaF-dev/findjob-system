<h1><%= Vacancy.model_name.human(count: 2) %></h1>

<% if @vacancies.empty? %>
  <div class="warning">Nenhuma vaga disponível</div>
<% else %>
    <dl>
    <% @vacancies.each do |vacancy| %>
        <dt><%= Vacancy.human_attribute_name("title") %></dt>
        <dd><%= link_to vacancy.title, vacancy_path(vacancy) %></dd>
        <dt><%= Vacancy.human_attribute_name("nivel") %></dt>
        <dd><%= vacancy.nivel %></dd>
        <dt><%= Company.model_name.human(count: 1) %></dt>
        <dd><%= vacancy.company.name %></dd>
        <hr>
    <% end %>
    </dl>
<% end %>

<%= link_to t("misc.back"), root_path %>
<div class="container p-4">  
  <h2 class="text-center text-black-50"><%= t(".welcome_message") %></h2>

  <div class="row justify-content-center">
    <div class="col-12 col-md-8 col-lg-6 p-3">
      <%= form_with url: search_path, method: :get do %>
        <div class="input-group">
          <%= label_tag :search, "" %>
          <%= text_field_tag :search, "", placeholder: t(".placeholder_text"), class: "form-control rounded" %>
          <%= submit_tag t(".search"), class: "btn btn-outline-secondary" %>
        </div>
      <% end %>
    </div>
  </div>
<div>